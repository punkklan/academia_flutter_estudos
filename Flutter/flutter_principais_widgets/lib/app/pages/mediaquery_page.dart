import 'package:flutter/material.dart';

class MediaQueryPage extends StatelessWidget {
  static String routerName = 'media1';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Media Query'),
      ),
    );
  }
}
