import 'package:flutter/material.dart';

class MediaQueryPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Media Query 2'),
        backgroundColor: Colors.amber,
      ),
    );
  }
}
