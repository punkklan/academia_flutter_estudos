import 'package:flutter/material.dart';

class ListasPage extends StatelessWidget {
  static String routerName = '/listas';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: Text('Listas Page'))),
      body: Container(),
    );
  }
}
